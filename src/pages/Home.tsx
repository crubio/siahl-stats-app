import type { FunctionComponent } from "../common/types";

export const Home = (): FunctionComponent => {

	return (
		<div className="container">
      <p>
        {"<player search components here>"}
      </p>
		</div>
	);
};
